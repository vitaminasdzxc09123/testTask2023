import { StyleSheet } from 'react-native';

export default function styles({}) {
    return (
        StyleSheet.create({
            container: {
              alignItems : 'center'
            },
            bottomButton: {
               width:'100%',
                marginTop : 50
            },
        })
    );
}
